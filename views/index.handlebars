<nav class="navbar navbar-expand-lg navbar-light">
    <form action="" method="GET">
        <a class="navbar-brand" href="#">Daily Onion</a>
    </form>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggåler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <form action="/saved" method="GET">
                <a class="nav-item nav-link" href="/saved">Saved Articles</a>
            </form>
        </div>
    </div>
</nav>

<div class="jumbotron">
    <div class="container" id="jumbo-text">
        <h1 class="display-3 text-center jumbo-header">Daily Onion</h1>
        <p class="p-lead text-center">This is your number one source to get your daily fix on the latest Onion Articles</p>
        <p class="lead text-center">
            <form action="/scrape" method="GET">
            <a class="btn btn-success btn-lg" href="/scrape" role="button" id="scan">Scan New Articles</a>
            </form>
        </p>
    </div>
</div>

<div id="results">

    {{#each articles}}
        <div class="container" id="article" data-id={{_id}} >
            <a href={{link}} target="_blank"><h3>{{title}}</h3></a>
            <button class="btn btn-success" id="articleBtn" type="submit">Save Article</button>
            <span>{{date}}</span>
        </div>
    {{/each}}
</div>

<script type="text/javascript">


$(document).on("click", "#article", function(){

    var Articleid = $(this).attr("data-id");

    console.log(Articleid)

    $.ajax({
        method: "POST",
        url: "/save/" + Articleid,
        data: {
            id: Articleid
        }
    }).done(function(data) {
        location.reload();
    });
})
</script>